version: 2.1
jobs:
  compile:
   docker:
      - image: wizardprjkt/build:kernel
   steps:
      - run:
          command: |
            git clone --depth=1 --recursive https://github.com/Kneba/kernel_asus_sdm660 -b onyx --single-branch kernel && cd kernel
            export TZ=Asia/Jakarta
            bash build.sh
          no_output_timeout : 1h
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
